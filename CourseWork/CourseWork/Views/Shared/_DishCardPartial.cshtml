@model DishResponce

<form class="dish-card" method="GET" action=@Url.Action("EditDish", "Dishes")>
    <input type="hidden" name="dishId" value="@Model.DishId" />
    <input type="hidden" name="returnUrl" value="@Url.Action("Dishes", "Dishes")" />
    <h4>@Model.DishName</h4>
    <button class="submit-button">@Model.DishPrice₴</button>
</form>